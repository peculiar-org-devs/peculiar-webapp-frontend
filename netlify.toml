[build]
publish = "dist"
command = "yarn run build"

# Proxy API calls to backend
[[redirects]]
from = "/api/*"
to = "https://peculiar-webapp-backend.onrender.com/api/:splat"
status = 200
force = true

# SPA fallback
[[redirects]]
from = "/*"
to = "/index.html"
status = 200
force = false
